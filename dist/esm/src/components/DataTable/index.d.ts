import { DataTableProps } from "./DataTable.types";
declare const DataTable: <T>({
  itemActions,
  columns,
  onSelect,
  records,
  isLoading,
  loadingMessage,
  entityName,
  selectedItems,
  idField,
  customEmptyMessage,
  customPaperHeight,
  columnsSelector,
  textSelectable,
  columnsShown,
  onColumnChange,
  infiniteScrollConfig,
  autoScrollToBottom,
  onSelectAll,
  rowStyle,
  parentClassName,
  disabled,
  sx,
  rowHeight,
  sortEnabled,
  sortCallBack,
}: DataTableProps<T>) => import("@emotion/react/jsx-runtime").JSX.Element;
export default DataTable;
