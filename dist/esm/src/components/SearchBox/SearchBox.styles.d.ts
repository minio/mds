import { Theme } from "@emotion/react";
export declare const searchBoxContainerStyles: (
  theme: Theme,
) => import("@emotion/react").SerializedStyles;
export declare const searchBoxBaseStyles: (
  theme: Theme,
) => import("@emotion/react").SerializedStyles;
