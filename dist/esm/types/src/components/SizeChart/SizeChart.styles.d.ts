import { Theme } from "@emotion/react";
export declare const sizeChartBaseStyles: (
  theme: Theme,
  usedBytes: number,
  totalBytes: number,
  chartLabel?: string,
) => import("@emotion/react").SerializedStyles;
