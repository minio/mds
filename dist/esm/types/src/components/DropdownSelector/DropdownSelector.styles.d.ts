import { Theme } from "@emotion/react";
export declare const dropdownBlockStyles: (
  theme: Theme,
) => import("@emotion/react").SerializedStyles;
export declare const dropdownItemStyles: (
  theme: Theme,
  hasIcon: boolean,
  hasIndicator: boolean,
) => import("@emotion/react").SerializedStyles;
